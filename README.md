# servicing Aircraft Queue

to run the client:
copy AircraftClient jar and lib to the target directory
run w/ localhost default or specify post and delete targets in the command line

ignore Memory* for now